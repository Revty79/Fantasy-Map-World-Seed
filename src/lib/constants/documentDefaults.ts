export const PROJECT_SCHEMA_VERSION = "1.0.0";

export const DEFAULT_WORLD_WIDTH = 8192;
export const DEFAULT_WORLD_HEIGHT = 4096;
export const DEFAULT_CHUNK_SIZE = 256;
export const DEFAULT_CELL_SIZE = 8;

export const DEFAULT_PROJECT_SETTINGS = {
  defaultWorldWidth: DEFAULT_WORLD_WIDTH,
  defaultWorldHeight: DEFAULT_WORLD_HEIGHT,
  defaultChunkSize: DEFAULT_CHUNK_SIZE,
  defaultCellSize: DEFAULT_CELL_SIZE,
};
